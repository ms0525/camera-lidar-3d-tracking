# Lightweight public dashboard dependencies. The live local mode additionally
# needs the project-root requirements and a backend-specific PyTorch install.
streamlit==1.59.1
plotly==6.9.0
numpy==2.2.6
pillow==12.2.0
